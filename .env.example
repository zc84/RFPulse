# PostgreSQL connection string
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/rfpulse

# JWT secret for auth tokens (generate a strong random string)
JWT_SECRET=change-me-to-a-secure-random-string

# Server port
PORT=3000
